# CRM Backend API URL
# For local development (VITE_ prefix for Vite build-time access)
VITE_API_URL=http://localhost:3000

# For GCP Cloud Run production - set this in Cloud Run environment variables
# This is read at RUNTIME by the server, not at build time
# API_URL=https://your-crm-backend-url.run.app
